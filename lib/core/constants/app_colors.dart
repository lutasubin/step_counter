import 'package:flutter/material.dart';

/// Màu sắc của ứng dụng
class AppColors {
  AppColors._();

  // Màu nền splash screen
  static const Color splashBackground = Color(0xFF1A1A2E);

  // Màu loading bar
  static const Color loadingBarActive = Color(0xFFF65738);
  static const Color loadingBarInactive = Color(0xFF253047);

  // Màu nút START
  static const Color startButton = Color(0xFFF65738);

  // Màu nền home
  static const Color homeBackground = Color(0xFF0C1220);
  static const Color cardBackground = Color(0xFF1B2539);
  static const Color metricsBackground = Color(0xFF111829);

  // Màu buttons
  static const Color buttonOrange = Color(0xFFF65738);
  static const Color buttonTeal = Color(0xFF10BFC3);
  static const Color buttonBlue = Color(0xFF2F6AFF);

  // Màu text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF717E97);
}
